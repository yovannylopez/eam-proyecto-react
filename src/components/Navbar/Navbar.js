import React from 'react';

import './estilos.sass';

const Navbar = () => {
  return (
    <nav>
      <a href="#">Posts</a>
      <a href="#">Albums</a>
      <a href="#">CRUD</a>
    </nav>
  );
};

export default Navbar;
